# PowerPoint 생성·검수 임시 산출물
.ppt-build/
.tmp_*/
tmp/
*.inspect.ndjson
slide-*.png

# 운영체제 및 편집기 임시 파일
.DS_Store
Thumbs.db
~$*.pptx
*.tmp

# 프론트엔드
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.next/
frontend/.env
frontend/.env.local
frontend/.env.*.local

# 발표 자료 바이너리 (저장소 미포함, 로컬/Release 로 관리)
*.pptx

# 발표 산출물 작업 폴더 (로컬 전용, 저장소 미포함)
output/

# 로컬 백업 아카이브
_v.tgz
*.tgz

# 에이전트 로컬 설정
**/.claude/settings.local.json

# backend (NestJS + Prisma)
backend/node_modules/
backend/dist/
backend/.env
backend/.env.local
backend/.env.*.local
backend/src/generated/
backend/coverage/
backend/*.tsbuildinfo

# DB 백업 덤프 (기본 저장 위치는 저장소 밖이지만, 실수로 안에 만들어도 막는다)
*.dump

# smoke 스크립트 로그
backend/smoke.log
